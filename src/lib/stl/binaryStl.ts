export const geometryToBinaryStl = (geometry: any): Uint8Array => {
  // Implementation of geometryToBinaryStl goes here
  return new Uint8Array(); // Return a default value or handle the case where no value is returned
};